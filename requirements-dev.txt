# Test-only dependencies. Not installed in the Docker image (see Dockerfile) -
# install locally with: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
mongomock>=4.1.0
httpx>=0.27.0
